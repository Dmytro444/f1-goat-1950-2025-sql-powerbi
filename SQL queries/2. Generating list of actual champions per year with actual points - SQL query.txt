SELECT year Year,
       d.name Name,
       position_number [Pos.],
       sd.total_race_starts [Races Started],
       sd.total_points [Pts.]
  FROM season_driver sd
       JOIN
       driver d ON sd.driver_id = d.id
 WHERE position_number = 1;
